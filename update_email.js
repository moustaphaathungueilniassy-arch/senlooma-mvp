const fs = require("fs"); const content = `import { Resend } from "resend"; import { render } from "@react-email/render"; import NotificationEmail from "@/emails/NotificationEmail"; import ResetPasswordEmail from "@/emails/ResetPasswordEmail"; const resend = new Resend(process.env.RESEND_API_KEY); const FROM_EMAIL = "onboarding@resend.dev"; export async function sendResetPasswordEmail(to: string, resetLink: string) { if (!process.env.RESEND_API_KEY) { return { success: false }; } try { const html = await render(ResetPasswordEmail({ resetLink })); const data = await resend.emails.send({ from: \`SAMA-DARAAL <${FROM_EMAIL}>\`, to: [to], subject: "Réinitialisation de votre mot de passe", html, }); return { success: true, data }; } catch (error) { return { success: false, error }; } } export async function sendNotificationEmail({ to, title, message, ctaText, ctaUrl }: { to: string; title: string; message: string; ctaText?: string; ctaUrl?: string; }) { if (!process.env.RESEND_API_KEY) { return { success: false }; } try { const html = await render(NotificationEmail({ title, message, ctaText, ctaUrl })); const data = await resend.emails.send({ from: \`SAMA-DARAAL <${FROM_EMAIL}>\`, to: [to], subject: title, html }); return { success: true, data }; } catch (error) { return { success: false, error }; } }`; fs.writeFileSync("src/lib/email.ts", content, "utf8");
